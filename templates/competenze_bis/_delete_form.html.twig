<form method="post" action="{{ path('app_competenze_bis_delete', {'id': competenze_bi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ competenze_bi.id) }}">
    <button class="btn">Delete</button>
</form>
