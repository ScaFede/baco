<form method="post" action="{{ path('app_citta_delete', {'id': cittum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cittum.id) }}">
    <button class="btn">Delete</button>
</form>
