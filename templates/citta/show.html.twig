{% extends 'base.html.twig' %}

{% block title %}Citta{% endblock %}

{% block body %}
    <h1>Citta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cittum.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ cittum.nome }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_citta_index') }}">back to list</a>

    <a href="{{ path('app_citta_edit', {'id': cittum.id}) }}">edit</a>

    {{ include('citta/_delete_form.html.twig') }}
{% endblock %}
