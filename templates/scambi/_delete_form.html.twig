<form method="post" action="{{ path('app_scambi_delete', {'id': scambi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scambi.id) }}">
    <button class="btn">Delete</button>
</form>
