<form method="post" action="{{ path('app_conoscenze_delete', {'id': conoscenze.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conoscenze.id) }}">
    <button class="btn">Delete</button>
</form>
