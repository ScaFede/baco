<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Baco - il baratto delle conoscenze{% endblock %}</title>

        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <!-- <link  rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous"> -->

         <link rel="stylesheet" href="{{ asset('css/bootstrap-italia.min.css')}}">

         <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:wght@300;400&family=Lato:wght@300;400;700;900&display=swap" rel="stylesheet">
        <link rel="text/plain"  href="{{ asset('css/bootstrap-italia.min.css.map')}}">
        <link rel="stylesheet" href="https://italia.github.io/design-comuni-pagine-statiche/assets/css/style.css">
        <link rel="stylesheet" href="{{ asset('css/fedestyle.css')}}">

            {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="">





<header class="it-header-wrapper it-header-sticky  mb-4" data-bs-toggle="sticky" data-bs-position-type="fixed" data-bs-sticky-class-name="is-sticky" data-bs-target="#header-nav-wrapper" style="">
<div class="it-header-slim-wrapper">
    <div class="container">
      <div class="row">
        <div class="col-12">
          <div class="it-header-slim-wrapper-content">
            <a class="d-none d-lg-block navbar-brand" href="{{ path('app_homepage') }}">BaCo</a>
            <div class="nav-mobile">
              <nav aria-label="Navigazione secondaria">
                <a class="it-opener d-lg-none" data-bs-toggle="collapse" href="#menuC1" role="button" aria-expanded="false" aria-controls="menuC1">
                  <span>Baco</span>
                  <svg class="icon" aria-hidden="true">
                    <use href="/dist/svg/sprites.svg#it-expand"></use>
                  </svg>
                </a>
                <div class="link-list-wrapper collapse" id="menuC1">
                  <ul class="link-list">
                    <li><a class="dropdown-item list-item" href="{{ path('app_competenze_bis_index') }}">Competenze</a></li>
                    <!-- <li><a class="list-item" href="{{ path('app_scambi_index') }}" aria-current="page">Scambi</a></li> -->
                    <li><a class="list-item " href="{{ path('app_user_index') }}" aria-current="page">Community</a></li>
                    <li><a class="dropdown-item list-item" href="#">Progetto</a></li>
                    <li><a class="list-item active" href="#" aria-current="page">Faq</a></li>
                  </ul>
                </div>
              </nav>
            </div>
            <div class="it-header-slim-right-zone">

              <div class="it-access-top-wrapper">

                <!-- Controlla se l'utente è autenticato -->
                {% if app.user %}

                <!-- <a href="{{ path('app_user_profile', {'id': app.user.id}) }}">Ciao, {{ app.user.nickname }}!</a> -->
                    <a class="btn btn-primary btn-icon btn-sm" href="{{ path('app_user_profile', {'id': app.user.id}) }}" title="Accedi all'area personale" aria-label="Accedi all'area personale" data-focus-mouse="false">
                      <span class="rounded-icon">
                        <svg class="icon icon-primary">
                          <use xlink:href="/bootstrap-italia/dist/svg/sprites.svg#it-user"></use>
                        </svg>
                      </span>
                      <span class="d-none d-lg-block">{{ app.user.nickname }}</span>
                    </a>
                    <a href="{{ path('app_logout') }}" type="button" class="btn btn-outline me-2 btn-sm">Esci</a>
                {% else %}




                    <a href="{{ path('app_login') }}" type="button" class="btn btn-primary  btn-sm">Accedi</a>
                    <a href="{{ path('app_register') }}" type="button" class="btn   btn-outline-primary btn-sm">Registrati</a>

                {% endif %}

                <!-- <a class="btn btn-primary btn-sm" href="#" data-focus-mouse="false">Accedi</a> -->
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</header>

    <main class="container">
      {% block body %}{% endblock %}

    </main>



    </body>
</html>
